# SQLite -> PostgreSQL Canli Sema Fark Raporu

- Tarih: 2026-02-26 21:30:30
- Kaynak SQLite: `backup/market-etiket-sistemi-26022026-postgresql-oncesi/database/omnex.db`
- Hedef PostgreSQL env: `.env.local`
- SQLite tablo sayisi: `89`
- PostgreSQL tablo sayisi (sistem disi): `89`
- Eslesen tablo sayisi: `89`

## Eksik Tablolar (SQLite var, PostgreSQL yok)
- Yok

## Eksik Kolonlar (SQLite var, PostgreSQL tablo icinde yok)
- Yok

## Riskli Tip Uyumsuzluklari
- `audit_logs.archived_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `audit_logs.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `audit_logs.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branch_import_logs.branch_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branch_import_logs.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branch_import_logs.completed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `branch_import_logs.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `branch_import_logs.created_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branch_import_logs.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branch_import_logs.started_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `branch_price_history.branch_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branch_price_history.changed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `branch_price_history.changed_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branch_price_history.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branch_price_history.product_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branches.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branches.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `branches.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branches.is_active` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `branches.is_virtual` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `branches.manager_user_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branches.parent_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `branches.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_branch_overrides.branch_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_branch_overrides.bundle_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_branch_overrides.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_branch_overrides.created_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_branch_overrides.deleted_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_branch_overrides.deleted_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_branch_overrides.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_branch_overrides.is_available` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `bundle_branch_overrides.previous_price_updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_branch_overrides.price_override` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `bundle_branch_overrides.price_updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_branch_overrides.price_valid_from` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_branch_overrides.price_valid_until` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_branch_overrides.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_branch_overrides.updated_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_branch_price_history.branch_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_branch_price_history.bundle_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_branch_price_history.changed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_branch_price_history.changed_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_branch_price_history.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_items.bundle_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_items.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_items.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_items.product_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_items.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_price_history.bundle_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundle_price_history.changed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_price_history.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundle_price_history.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundles.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundles.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundles.created_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundles.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `bundles.previous_price_updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundles.price_override` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `bundles.price_updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundles.price_valid_from` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundles.price_valid_until` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundles.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundles.valid_from` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `bundles.valid_until` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `categories.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `categories.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `categories.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `categories.is_default` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `categories.is_demo` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `categories.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `companies.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `companies.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `companies.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `company_storage_usage.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `company_storage_usage.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `company_storage_usage.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `company_storage_usage.last_calculated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `company_storage_usage.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_alerts.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_alerts.device_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_alerts.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_commands.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_commands.device_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_commands.executed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_commands.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_content_assignments.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_content_assignments.device_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_content_assignments.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_group_members.device_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_group_members.group_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_group_members.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_groups.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_groups.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_groups.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_groups.parent_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_groups.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_heartbeats.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_heartbeats.device_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_heartbeats.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_logs.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_logs.device_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_logs.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_sync_requests.approved_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_sync_requests.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_sync_requests.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_sync_requests.expires_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_sync_requests.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_sync_requests.last_request_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_sync_requests.touch_support` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `device_sync_requests.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_tokens.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_tokens.device_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_tokens.expires_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_tokens.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `device_tokens.is_revoked` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `device_tokens.last_used_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `device_tokens.revoked_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `devices.approved_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `devices.branch_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `devices.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `devices.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `devices.current_template_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `devices.group_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `devices.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `devices.last_heartbeat` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `devices.last_online` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `devices.last_seen` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `devices.last_stream_request_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `devices.last_sync` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `devices.stream_mode` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `devices.sync_code_expires_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `devices.token_expires_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `devices.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `firmware_updates.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `firmware_updates.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `firmware_updates.created_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `firmware_updates.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `firmware_updates.is_active` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `firmware_updates.is_mandatory` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `firmware_updates.released_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `firmware_updates.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `gateway_commands.completed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `gateway_commands.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `gateway_commands.device_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `gateway_commands.expires_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `gateway_commands.gateway_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `gateway_commands.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `gateway_commands.sent_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `gateway_devices.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `gateway_devices.device_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `gateway_devices.gateway_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `gateway_devices.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `gateway_devices.last_seen` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `gateway_devices.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `gateways.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `gateways.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `gateways.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `gateways.last_heartbeat` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `gateways.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hal_distribution_logs.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hal_distribution_logs.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hal_distribution_logs.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hal_distribution_logs.product_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hanshow_aps.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hanshow_aps.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hanshow_aps.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hanshow_aps.last_seen_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hanshow_aps.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hanshow_esls.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hanshow_esls.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hanshow_esls.current_product_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hanshow_esls.current_template_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hanshow_esls.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hanshow_esls.last_heartbeat_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hanshow_esls.last_sync_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hanshow_esls.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hanshow_firmwares.cached_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hanshow_queue.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hanshow_queue.completed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hanshow_queue.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hanshow_queue.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hanshow_queue.processed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hanshow_queue.product_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hanshow_queue.template_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hanshow_settings.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `hanshow_settings.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `hanshow_settings.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `import_mappings.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `import_mappings.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `import_mappings.is_default` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `import_mappings.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `integration_settings.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `integration_settings.is_active` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `integration_settings.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `integration_settings_audit.changed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `integration_settings_audit.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `integrations.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `integrations.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `integrations.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `integrations.last_sync` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `integrations.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `label_sizes.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `label_sizes.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `label_sizes.is_active` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `label_sizes.is_default` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `label_sizes.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `layout_configs.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `layout_configs.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `layout_configs.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `license_device_pricing.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `license_device_pricing.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `license_device_pricing.license_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `license_device_pricing.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `license_plans.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `license_plans.is_active` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `license_plans.is_enterprise` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `license_plans.is_popular` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `license_plans.is_unlimited` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `license_plans.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `licenses.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `licenses.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `licenses.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `licenses.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `licenses.valid_from` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `licenses.valid_until` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `media.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `media.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `media.folder_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `media.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `media.is_public` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `media.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `media.uploaded_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `media_folders.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `media_folders.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `media_folders.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `media_folders.parent_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `media_folders.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `menu_items.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `menu_items.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `menu_items.visible` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `migrations.executed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `mqtt_settings.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `mqtt_settings.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `mqtt_settings.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `mqtt_settings.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `mqtt_settings.use_tls` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `notification_recipients.archived_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `notification_recipients.deleted_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `notification_recipients.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `notification_recipients.notification_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `notification_recipients.read_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `notification_settings.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `notification_settings.enabled` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `notification_settings.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `notification_settings.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `notifications.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `notifications.expires_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `notifications.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `payment_settings.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `payment_settings.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `payment_settings.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `payment_settings.is_active` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `payment_settings.is_test_mode` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `payment_settings.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `payment_transactions.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `payment_transactions.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `payment_transactions.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `payment_transactions.license_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `payment_transactions.paid_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `payment_transactions.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `payment_transactions.user_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `permissions.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `permissions.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `playlist_items.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `playlist_items.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `playlist_items.muted` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `playlist_items.playlist_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `playlists.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `playlists.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `playlists.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `playlists.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `price_history.changed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `price_history.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `price_history.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `price_history.product_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_branch_hal_overrides.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_branch_hal_overrides.deleted_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_branch_hal_overrides.hal_data_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_branch_hal_overrides.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_branch_hal_overrides.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_branch_overrides.branch_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_branch_overrides.campaign_end` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_branch_overrides.campaign_start` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_branch_overrides.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_branch_overrides.created_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_branch_overrides.deleted_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_branch_overrides.deleted_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_branch_overrides.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_branch_overrides.is_available` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `product_branch_overrides.price_updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_branch_overrides.price_valid_until` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_branch_overrides.product_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_branch_overrides.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_branch_overrides.updated_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_hal_data.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_hal_data.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_hal_data.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_hal_data.product_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_hal_data.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_renders.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_renders.completed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_renders.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `product_renders.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_renders.product_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `product_renders.template_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `production_types.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `production_types.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `production_types.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `production_types.is_default` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `production_types.is_demo` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `production_types.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `products.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `products.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `products.erp_updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `products.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `products.is_default` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `products.is_demo` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `products.is_featured` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `products.last_rendered_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `products.previous_price_updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `products.price_updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `products.price_valid_until` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `products.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `products.valid_from` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `products.valid_until` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `products.vat_updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_cache.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_cache.expires_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_cache.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `render_cache.rendered_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_cache.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_jobs.completed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_jobs.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_jobs.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `render_jobs.scheduled_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_jobs.started_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_queue.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `render_queue.completed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_queue.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_queue.created_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `render_queue.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `render_queue.last_retry_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_queue.next_retry_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_queue.product_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `render_queue.scheduled_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_queue.started_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_queue.template_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `render_queue.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_queue_items.completed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_queue_items.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_queue_items.device_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `render_queue_items.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `render_queue_items.next_retry_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `render_queue_items.queue_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `render_queue_items.started_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `schedule_devices.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `schedule_devices.schedule_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `schedules.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `schedules.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `schedules.end_date` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `schedules.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `schedules.start_date` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `schedules.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `sessions.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `sessions.expires_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `sessions.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `sessions.user_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `settings.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `settings.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `settings_backup.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `settings_backup.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `stream_access_logs.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `tamsoft_depo_mapping.branch_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `tamsoft_depo_mapping.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `tamsoft_depo_mapping.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `tamsoft_depo_mapping.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `tamsoft_depo_mapping.is_default_region` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `tamsoft_depo_mapping.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `tamsoft_settings.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `tamsoft_settings.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `tamsoft_settings.enabled` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `tamsoft_settings.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `tamsoft_settings.last_sync_date` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `tamsoft_settings.only_ecommerce` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `tamsoft_settings.only_stock_positive` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `tamsoft_settings.single_barcode` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `tamsoft_settings.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `tamsoft_sync_logs.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `tamsoft_sync_logs.completed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `tamsoft_sync_logs.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `tamsoft_sync_logs.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `tamsoft_sync_logs.started_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `tamsoft_tokens.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `tamsoft_tokens.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `tamsoft_tokens.expires_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `tamsoft_tokens.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `templates.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `templates.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `templates.created_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `templates.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `templates.is_default` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `templates.is_demo` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `templates.is_forked` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `templates.is_public` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `templates.parent_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `templates.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `templates_backup.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `templates_backup.is_default` -> sqlite `INT` (integer), pg `boolean(bool)` (boolean)
- `templates_backup.is_demo` -> sqlite `INT` (integer), pg `boolean(bool)` (boolean)
- `templates_backup.is_forked` -> sqlite `INT` (integer), pg `boolean(bool)` (boolean)
- `templates_backup.is_public` -> sqlite `INT` (integer), pg `boolean(bool)` (boolean)
- `templates_backup.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `transcode_queue.completed_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `transcode_queue.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `transcode_queue.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `transcode_queue.started_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `transcode_queue.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `transcode_variants.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `transcode_variants.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `transcode_variants.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `user_branch_access.branch_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `user_branch_access.expires_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `user_branch_access.granted_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `user_branch_access.granted_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `user_branch_access.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `user_branch_access.is_default` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `user_branch_access.user_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `user_notification_preferences.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `user_notification_preferences.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `user_notification_preferences.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `user_notification_preferences.user_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `users.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `users.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `users.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `users.last_login` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `users.password_reset_expires` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `users.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_template_assignments.assigned_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_template_assignments.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_template_assignments.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_template_assignments.device_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_template_assignments.end_date` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_template_assignments.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_template_assignments.is_active` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `web_template_assignments.last_synced_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_template_assignments.start_date` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_template_assignments.template_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_template_assignments.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_template_versions.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_template_versions.created_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_template_versions.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_template_versions.template_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_template_widgets.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_template_widgets.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_template_widgets.created_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_template_widgets.is_active` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `web_template_widgets.is_system` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `web_template_widgets.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_templates.company_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_templates.created_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_templates.created_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_templates.deleted_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_templates.id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_templates.is_forked` -> sqlite `INTEGER` (integer), pg `boolean(bool)` (boolean)
- `web_templates.parent_template_id` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)
- `web_templates.published_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_templates.updated_at` -> sqlite `TEXT` (text), pg `timestamp with time zone(timestamptz)` (datetime)
- `web_templates.updated_by` -> sqlite `TEXT` (text), pg `uuid(uuid)` (uuid)

